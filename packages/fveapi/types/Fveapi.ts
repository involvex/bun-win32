import type { Pointer } from 'bun:ffi';

export type { BOOL, BYTE, DWORD, HANDLE, HRESULT, LPCWSTR, LPDWORD, LPVOID, LPWSTR, NULL, PBYTE, PDWORD, PHANDLE, PVOID, ULONG } from '@bun-win32/core';

export enum FveConversionStatus {
  FVE_CONVERSION_STATUS_DECRYPTED = 0x0000_0000,
  FVE_CONVERSION_STATUS_DECRYPTION_IN_PROGRESS = 0x0000_0003,
  FVE_CONVERSION_STATUS_DECRYPTION_PAUSED = 0x0000_0005,
  FVE_CONVERSION_STATUS_ENCRYPTED = 0x0000_0001,
  FVE_CONVERSION_STATUS_ENCRYPTION_IN_PROGRESS = 0x0000_0002,
  FVE_CONVERSION_STATUS_ENCRYPTION_PAUSED = 0x0000_0004,
}

export enum FveEncryptionFlags {
  FVE_ENCRYPT_DATA_ONLY = 0x0000_0001,
  FVE_ENCRYPT_FULL = 0x0000_0000,
}

export enum FveLockStatus {
  FVE_LOCK_STATUS_LOCKED = 0x0000_0001,
  FVE_LOCK_STATUS_UNLOCKED = 0x0000_0000,
}

export enum FveProtectionStatus {
  FVE_PROTECTION_STATUS_OFF = 0x0000_0000,
  FVE_PROTECTION_STATUS_ON = 0x0000_0001,
  FVE_PROTECTION_STATUS_UNKNOWN = 0x0000_0002,
}

export type FVE_HANDLE = bigint;

export type PFVE_HANDLE = Pointer;
